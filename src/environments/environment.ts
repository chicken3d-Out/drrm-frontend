export const environment = {
  production: false,
  // Empty string = same-origin relative requests, proxied to localhost:4000
  // by proxy.conf.json during `npm start`.
  apiBase: '',
  socketBase: ''
};
